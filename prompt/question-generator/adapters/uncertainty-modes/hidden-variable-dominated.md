UNCERTAINTY MODE — HIDDEN-VARIABLE DOMINATED

Research behavior:
- search for proxies
- triangulate aggressively
- widen the hypothesis set

Risk:
- overfitting to indirect clues

## Stage Relevance

Routing — Primary
- Classifies the problem as one where key drivers are only partially observable.

Boundary — Light
- Can widen scope modestly to include latent states and proxy-bearing systems.

Structure — Primary
- Forces the stage to search for missing drivers, proxies, and latent states.

Scenarios — Primary
- Favors wider scenario sets and more humility about hidden branch drivers.

Question Generation — Primary
- Favors proxy questions and questions about what would reveal the hidden state.

Evidence Planning — Primary
- Requires triangulation, confidence penalties, and careful proxy handling.

Decision Logic — Primary
- Pushes action logic toward staging, reversibility, and wider uncertainty bands.

Signal Translation — Primary
- Signals should focus on aligned proxies rather than single direct claims.

Monitoring — Primary
- Monitoring should update only when multiple proxies align.

Render — Modulating
- Should show uncertainty explicitly and distinguish evidence from inference.
